<scroll-view scroll-x class="scroll-view">
  <view id="{{ idx }}" bindtap="goto" wx:for-index="idx" class="item {{  item.id === propType ? 'active':'' }}" wx:for="{{ propData }}" wx:key="{{ idx }}">{{ item.categoryName }}</view>
</scroll-view>